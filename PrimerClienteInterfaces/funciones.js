function isOdd(n){
    if(n.length>5) return str;
}

function strFive(str){
    if(str.length>5) return str;
}

function filter(a, f){
  let ys = [];
  for (let i of a){
    if(f(a[i])){
      ys.push(i);
    }
  }
  return ys;
}